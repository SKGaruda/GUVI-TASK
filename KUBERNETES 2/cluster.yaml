apiVersion: eksctl.io/v1alpha5
kind: ClusterConfig

metadata:
  name: nginx-eks-cluster
  region: ap-south-1

managedNodeGroups:
  - name: nginx-workers
    instanceType: t3.medium
    desiredCapacity: 2
    minSize: 1
    maxSize: 3
